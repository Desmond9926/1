# 问题3：全向干扰源的自动搜索、定位与清除

## 1 问题重述

目标区域为半径 \(R_0=1800\) m 的圆盘 \(B(O,R_0)\)。其中有 \(N\in\{10,\ldots,16\}\) 个全向干扰源，\(N\) 未知。干扰源占用 \(\{1,\ldots,20\}\) 中互不相同的频道，信号互不影响。一只机器狗从原点出发，测向机初始频道为 1，要求在**保证全部清除**的前提下使完成时间尽量短。

评价指标为

\[
\eta=\frac{N_{\mathrm{cleared}}}{N},\qquad
\bar t=\frac{T_{\mathrm{total}}}{N_{\mathrm{cleared}}},
\]

其中 \(T_{\mathrm{total}}\) 包含移动、频道切换、检测、光学精确定位和激光清除。正式测试按题目表 1 填写清除个数、平均定位清除时间和程序运行时间。

约束来自题目附录 2 与附件 1/2：

| 量 | 取值 | 说明 |
|---|---|---|
| 测向误差 | \(\Delta=1^\circ\) | 同点重复不变，异地才改变 |
| 有效接收半径 \(R\) | \([1000,1500]\) m | 每源不同，接口不返回 |
| 移动 | \(5\) m/s 直线 | 由相邻指令位置差推断 |
| 检测 | \(5\) s | 必须停止；`/measure` |
| 换频 | \(1\) s | 仅相邻两次 `/measure` 频道不同时发生 |
| 光学+清除 | 未发现 \(3\) s，成功 \(5\) s | `/clear`，半径 \(20\) m，**不切换测向频道** |
| 过近 | \(\le 5\) m | 返回 `near`，无示向度，可直接清除 |

检测三态为 `no_signal` / `near` / `direction`。全向下，`no_signal` 的保守含义是：该点 \(1000\) m 内没有该频道未清除源。

光学半径只有 \(20\) m。若用 \(20\) m 网格扫全圆，路径长度约 \(\pi R_0^2/40\approx 2.5\times 10^5\) m，仅移动约 \(14\) 小时，不可行。必须采用**测向覆盖 \(\to\) 交会缩小 \(\to\) 光学清除**的分层策略。

---

## 2 符号与时间模型

### 2.1 频道状态

对每个频道 \(c\in\{1,\ldots,20\}\) 维护：

- `unknown`：尚未收到该频道信号，可行集 \(\Omega_c\subseteq B(O,R_0)\)；
- `tracked`：至少一次 `direction`，并保存测向历史 \(\{(S_i,\theta_i)\}\)；
- `cleared`：`/clear` 返回 `success`；
- `empty`：最坏接收覆盖下可行集已空，判定该频道无源。

初始全部为 `unknown`，\(\Omega_c=B(O,1800)\)。

### 2.2 集员更新

在点 \(S\) 对频道 \(c\) 做 `/measure`：

- `no_signal`：\(\Omega_c\leftarrow\Omega_c\setminus B(S,1000)\)；
- `direction` \(\theta\)：\(\Omega_c\leftarrow\Omega_c\cap\operatorname{Sector}(S,\theta,\Delta)\cap B(S,1500)\)；
- `near`：\(\Omega_c\leftarrow\Omega_c\cap B(S,5)\)，随即在 \(S\) 清除。

`/clear` 在 \(P\) 成功则该频道改为 `cleared`；失败则 \(\Omega_c\leftarrow\Omega_c\setminus B(P,20)\)。

交会多边形的构造与直径计算复用问题 1：每个误差扇形写成两个闭半平面，半平面交为凸多边形，直径由顶点对达到。

### 2.3 虚拟时间

相邻动作位置 \(P_{k-1}\to P_k\)，测向频道 \(ch_{k-1}\to ch_k\)：

\[
T_{\mathrm{move}}=\frac{\|P_k-P_{k-1}\|}{5},\qquad
T_{\mathrm{sw}}=\mathbf{1}[\text{本次为 measure 且 }ch_k\neq ch_{k-1}],
\]

\[
T_{\mathrm{act}}=
\begin{cases}
5, & \text{measure},\\
3, & \text{clear 未发现},\\
5, & \text{clear 成功}.
\end{cases}
\]

目标是在 \(\eta=1\) 的硬约束下最小化 \(T_{\mathrm{total}}\)。

---

## 3 问题拆解与文献对应

| 子问题 | 内容 | 借鉴 | 本题用法 |
|---|---|---|---|
| A 覆盖搜索 | 用尽量少的检测点保证任意位置都能被 \(1000\) m 收到 | Kershner 圆覆盖；Galceran & Carreras 覆盖规划综述（2013） | 原点 + 半径 \(1250\) m 的正六边形 6 点，不是 \(20\) m 割草 |
| B 换频调度 | \(20\) 频道、切换 \(1\) s | SpecWatch（带切换代价的频谱监测，2018） | 必须覆盖全部频道；原点全扫，其余点只扫剩余未知频道 |
| C 单源交会 | 第一次示向后选第二检测点 | Bishop 等 *Automatica* 2010；Doğançay *Sensors* 2022；问题 2 | 解析点 \(S_1+750\boldsymbol u\pm 600\boldsymbol n\) |
| D 光学清除 | 交会多边形能否一次落入 \(20\) m | 问题 1；Welzl 最小包围圆；Jung 定理 | MEC 半径 \(\le 20\) 则中心一枪，否则沿直径与顶点补枪；过大则第三次测向 |
| E 多源排序 | 已发现多源时先清谁 | TSP with neighborhoods | 按示向度极角排序，从最近的第二检测点起沿圆周访问 |
| F 停–测–走 | 不能边走边测 | Hoffmann, *Sensor Path Planning for Emitter Localization*, UCL, 2023 | 普查点才扫多个频道，定位阶段只测目标频道 |
| G 未知个数终止 | \(N\) 未知 | Yousuf 等未知个数多目标搜索（*Sensors* 2024） | 频道全部 `cleared`/`empty` 或已清 \(16\) 个则退出 |

主算法采用确定性分层，不使用强化学习，也不使用场强测距（接口不给 RSS）。

---

## 4 最坏接收覆盖

### 4.1 完备性条件

**命题.** 若检测点集 \(\mathcal S\) 满足

\[
B(O,1800)\subseteq \bigcup_{S\in\mathcal S} B(S,1000),
\]

则对任意全向源（无论 \(R\in[1000,1500]\)、无论频道），必存在某 \(S\in\mathcal S\)，使该点对该频道的 `/measure` 不是 `no_signal`。

证明：真实源 \(G\) 满足 \(\|G\|\le 1800\)。由覆盖条件存在 \(S\) 使 \(\|S-G\|\le 1000\le R\)，全向下必能收到信号。

原点单独覆盖内盘 \(B(O,1000)\)。剩余是环带 \([1000,1800]\)。

### 4.2 正六边形一圈

取 \(\rho=1250\) m，

\[
S_k=\bigl(1250\cos\tfrac{k\pi}{3},\, 1250\sin\tfrac{k\pi}{3}\bigr),\quad k=0,\ldots,5.
\]

环带外缘最坏点位于 \(r=1800\)、两相邻站的角平分线方向。该点到最近站的距离为

\[
d=\sqrt{1800^2+1250^2-2\cdot 1800\cdot 1250\cdot\cos 30^\circ}\approx 951.5\ \mathrm{m}<1000\ \mathrm{m}.
\]

内缘 \(r=1000\) 已被原点覆盖。故

\[
\mathcal S_0=\{O\}\cup\{S_0,\ldots,S_5\}
\]

是 \(7\) 点完备覆盖集。对规则单圈，\(n=5\) 时上述二次方程判别式为负，不存在能罩住外缘的半径；因此 **原点 + \(6\) 点环是最小规则构型**。这与经典“\(1+6\) 等圆覆盖一个较大圆”的构型一致。

机器狗可以走出目标圆（附件 2），但上述 \(7\) 点均在圆内。

---

## 5 三阶段策略

### 5.1 阶段 I：原点普查

在 \((0,0)\) 按频道 \(1,2,\ldots,20\) 依次 `/measure`。初始频道已是 \(1\)，第一次不换频。耗时

\[
20\times 5+19\times 1=119\ \mathrm{s}.
\]

扫完再离开：原点移动代价为零，提前离开会增加回头成本。

- `direction`：记录 \((O,\theta_c)\)，频道改为 `tracked`；
- `near`：立刻 `/clear`；
- `no_signal`：该频道剩余区域收缩为环带。

若源在圆盘内均匀、\(R\) 在 \([1000,1500]\) 上均匀，原点听到的概率约为

\[
\frac{\mathbb E[R^2]}{R_0^2}=\frac{(1500^3-1000^3)/(3\times 500)}{1800^2}\approx 0.49.
\]

大约一半的源在阶段 I 被发现。

### 5.2 阶段 II：清除原点已跟踪源

对每个 `tracked` 频道执行单源子程序 `LocalizeClear`。访问顺序：按示向度极角排序，并从第二检测点离当前位置最近的源开始沿圆周行进。这是绕原点的圆周 TSP 近似，避免反复横切。

### 5.3 阶段 III：环带普查，即发现即清除

剩余 `unknown` 频道的源（若存在）必在环带。从当前点最近的六边形顶点起访问环点。在环点 \(S_k\) 只扫描**尚未判定空的未知频道**，并按当前测向频道做环形顺序以减少切换。

- `no_signal`：将该环点记入该频道的覆盖记录；若原点与 \(6\) 个环点均无信号，则频道为 `empty`；
- `direction`/`near`：立即 `LocalizeClear`（第一检测点就是当前环点），然后回到尚未完成的环点。

若某环点对当前未知频道已全部测过，则跳过。全部频道变为 `cleared` 或 `empty`，或已清除 \(16\) 个，则 `/exit`。

---

## 6 单源定位与清除

### 6.1 第二检测点（问题 2）

记第一次检测点为 \(S_1\)、示向度为 \(\theta_1\)，

\[
\boldsymbol u=(\cos\theta_1,\sin\theta_1),\qquad
\boldsymbol n=(-\sin\theta_1,\cos\theta_1).
\]

取问题 2 的稳健解析点

\[
S_2^\pm=S_1+750\boldsymbol u\pm 600\boldsymbol n.
\]

\(\|S_2-S_1\|\approx 960.5\) m。对完整 \(1500\) m 误差扇形，候选点到扇形顶点与远端角点的最大距离约为 \(976.9\) m \(<1000\) m，故二次接收有严格保障。交会角因横向 \(600\) m 偏移而远离 \(0^\circ/180^\circ\) 的病态区，与 Bishop 等“两站交会角接近 \(90^\circ\)”的几何结论同向。

两侧都可行时，选离当前位置较近的一侧；距离接近时选更靠近原点的一侧，以缩短后续路程。若该侧意外 `no_signal`，依次改用另一侧以及更近的偏置 \((400,\pm 350)\)、\((200,\pm 250)\)。

### 6.2 交会区域（问题 1）

两次（或三次）示向后，

\[
\Omega
=\bigcap_i \operatorname{Sector}(S_i,\theta_i,\Delta)
\cap B(S_i,1500)
\cap B(O,1800).
\]

用正多边形外包圆盘，再用半平面裁剪求交，得到凸多边形。计算顶点直径与最小包围圆 \((C,r)\)。

### 6.3 光学策略

- 若 \(r\le 20\)：到 \(C\) 一次 `/clear`，必中（凸集被半径 \(r\) 的圆覆盖）。
- 若 \(20<r\le 40\)：先到 \(C\)，失败则沿直径等距补点，并补尚未被 \(20\) m 覆盖的顶点。
- 若 \(r>40\)：在质心附近沿横向偏置约 \(150\) m 做第三次测向。距离缩短后直径近似按 \(d\tan\Delta\) 下降，通常 \(r\) 会降到 \(20\) m 以下。

`near` 出现时，当前位置已在 \(5\) m 内，直接 `/clear`。

### 6.4 子程序流程

```text
LocalizeClear(c, S1, θ1):
    若无测向且当前位置可清，则 clear 并返回
    按 (750,±600) 及更近偏置生成第二检测点序列
    在尚未测过的候选点 measure(c)
        near     -> 就地 clear
        direction -> 求 Ω，必要时第三次测向
        no_signal -> 试下一个候选点
    用 20 m 圆覆盖 Ω，依次 /clear，直到 success
```

---

## 7 算法总流程

```text
enter()                          # 原点、频道 1
for c = 1..20:
    measure((0,0), c)
    更新频道状态；若 near 则立即 clear
按示向度极角序 LocalizeClear 所有 tracked
while 存在 unknown 频道:
    取最近的、对该批未知频道尚未测完的环点 Sk
    按环形频道序在 Sk 扫描未知频道
    若听到信号: LocalizeClear，然后继续 while
    若 7 个普查点均无信号: 该频道 empty
exit()
```

复杂度：频道数 \(20\)、普查点 \(7\)、每源测向次数 \(O(1)\)、交会多边形顶点数 \(O(1)\)，整体与 \(N\) 成线性。虚拟时间主导项是移动，不是几何计算。现实时间上每次检测的 \(5\) s 只推进虚拟时钟，程序不必等待。

---

## 8 时间估计与离线演练

记 \(N=13\) 为中间值，约一半在原点被听到。

| 项 | 估计 |
|---|---|
| 原点全扫 | \(119\) s |
| 单源：第二点移动 \(\approx 960\) m（\(192\) s）+ 测向 \(5\) s + 前往源 \(\sim 100\)–\(200\) s + 清除 \(5\) s | 约 \(300\)–\(400\) s |
| \(N\) 个源 | \(\sim 4\times 10^3\) s |
| 环带周游与剩余频道扫描 | \(\sim 5\times 10^2\)–\(2\times 10^3\) s |

合计约 \(1.5\)–\(2\) 小时，远低于虚拟时限 \(100\) 小时。

用与题目同构的离线模拟器（示向度误差 \(\pm 1^\circ\)、接收半径 \([1000,1500]\)、计时规则与附件一致）做完备性检验：

| 场景 | 源个数 | 清除个数 | 总虚拟时间 / s | 平均每源 / s |
|---|---|---|---|---|
| 外缘最坏：\(6\) 个边界中点 \(R=1000\) + \(4\) 个内源 | \(10\) | \(10\) | \(4407\) | \(441\) |
| 随机 seed=1–42，\(n=10\)–\(16\) | \(10\)–\(16\) | 全部 | \(4.4\times 10^3\)–\(5.6\times 10^3\) | \(390\)–\(450\) |

外缘最坏构型专门把源放在 \(1790\) m、两环点角平分线上且 \(R=1000\) m，原点听不到，必须靠 \(951\) m 覆盖裕度捕获。全部场景 \(\eta=1\)。

第三次测向在 MEC 半径 \(>40\) m 时启用，离线中可把 \(r\) 从 \(50\)–\(110\) m 压到 \(10\) m 以下，再一枪清除。

---

## 9 程序与接口

机器狗程序只使用附件规定的四条指令：`POST /enter`、`/measure`、`/clear`、`/exit`。要点：

- `robot_id` 必须等于当前登录参赛队号；`arena_id` 固定为 `"default"`；
- 每个新动作使用新的 `request_id`，网络重试必须复用原请求；
- 串行等待响应，检查 HTTP 状态与 `accepted`；`accepted=false` 时不要把 `virtual_time_s=0` 当作当前时刻；
- `/clear` 不改变测向频道；
- `/enter` 返回的 `remaining_real_duration_s` 为实际可用现实时间；
- 程序自行记录指令与响应，写入 `q3_run_log.json`。

文件：

| 文件 | 作用 |
|---|---|
| `Q3/geometry.py` | 半平面交、直径、最小包围圆、第二检测点、光学覆盖点 |
| `Q3/robot_q3.py` | 三阶段策略与 HTTP 通信 |
| `Q3/offline_sim.py` | 离线物理引擎 |
| `Q3/test_q3.py` | 覆盖几何与多场景必清检验 |

连接模拟器：

```text
python robot_q3.py --url http://127.0.0.1:2026 --robot-id <参赛队号>
```

离线自检：

```text
python robot_q3.py --offline --seed 1 --n-sources 13
python test_q3.py
```

---

## 10 正式测试结果表（测完后填写）

| 测试案例编码 | 清除干扰源个数 | 平均定位清除时间 | 程序运行时间 |
|---|---|---|---|
| 测试 1 案例编码 |  |  |  |
| 测试 2 案例编码 |  |  |  |
| 测试 3 案例编码 |  |  |  |

演练测试结束后模拟器会给出干扰源总数，用于计算 \(\eta\)。正式测试不显示真值，但本策略在覆盖完备的前提下应以 \(\eta=1\) 结束。

---

## 11 小结

问题 3 被拆成覆盖搜索、换频、单源交会、光学清除和多源排序。覆盖用原点加六边形一圈保证最坏 \(R=1000\) m 下仍能听到每一个源；单源定位直接接入问题 1 的集员交会与问题 2 的 \((750,\pm 600)\) 选点；光学只处理直径数十米的凸多边形，避免全图细扫。未知个数通过“频道可行集被 \(7\) 个普查点挖空则判空”终止。离线最坏外缘与多组随机实例均全部清除，平均每源约 \(7\) 分钟量级。
